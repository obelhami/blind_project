# ——— Frontend (root .env) ———
# Vite only exposes variables prefixed with VITE_

# ElevenLabs API key – used for voice input (STT) and voice output (TTS) in the side chat.
# Get your key at https://elevenlabs.io/app/settings/api-keys
VITE_ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ElevenLabs Conversational AI agent ID (optional; for future agent integration).
# VITE_ELEVENLABS_AGENT_ID=agent_xxxxx

# Optional: voice used for TTS (default: Rachel). Find IDs in ElevenLabs Voice Library.
# VITE_ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Optional: API base URL. Leave empty in dev (Vite proxy). Set in production, e.g. https://api.example.com
# VITE_API_URL=
